{
  "name": "wx-font",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "fantasticon && yarn public",
    "public": "node ./build.js",
    "deploy": "yarn build && ./deploy.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fantasticon": "^1.2.2"
  },
  "devDependencies": {
    "glob": "^7.1.7"
  }
}
